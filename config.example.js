/*
  NEXUS — Configuration Example
  ================================
  1. Copy this file: cp config.example.js config.js
  2. Fill in your real Supabase credentials below
  3. config.js is in .gitignore — it will NEVER be committed

  WHERE TO FIND THESE VALUES:
  → Go to https://supabase.com → your project
  → Settings → API
  → Copy "Project URL" and "anon public" key
*/

window.NEXUS_CONFIG = {
  SUPABASE_URL:  'https://YOUR_PROJECT_ID.supabase.co',
  SUPABASE_ANON: 'YOUR_ANON_PUBLIC_KEY_HERE',
};
